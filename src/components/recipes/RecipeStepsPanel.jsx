// Deprecated: instructions now live inside each recipe's expand view.
// Keep this file to satisfy existing imports, but render nothing.

export default function RecipeStepsPanel() {
  return null;
}
